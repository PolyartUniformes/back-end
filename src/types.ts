type FactionType = {
  parent_uuid: string;
  faccionista: string;
  deliver_at: string;
  price: string;
  quantity: string;
  insumos: string;
  observation: string;
};

export type { FactionType };
